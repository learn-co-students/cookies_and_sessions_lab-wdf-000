<%= form_tag('/products/add', method: :post) %>
<%= label_tag 'product' %>
<%= text_field_tag :product %>
<%= submit_tag 'add to cart'%>

<hr />
<h1>Your Products</h1>
<ul>
  <% cart.each do |product| %>
    <li><%= product %></li>
  <% end %>
</ul>
